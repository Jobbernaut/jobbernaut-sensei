[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "jobbernaut-sensei"
version = "0.1.0"

[project.scripts]
sensei = "sensei:main"

[tool.setuptools]
package-dir = {"" = "src"}
py-modules = ["sensei", "mark", "new", "lopen", "revisit"]
